전체 코드를 처음부터 다시 읽겠습니다. 파일 하나하나 순서대로.

---

## 완전 재검토

### 파일 1: `chat.py`

**버그 1 (기존 식별): 이벤트 중복 전송** — 이미 수정안 제시함.

**버그 2 (신규): `_stream_backend_job_until_terminal`에서 terminal 상태 감지 후에도 이벤트 루프가 한 번 더 돌 수 있음**

```python
        for event in snap.get("events", []) or []:
            # ... 이벤트 처리 ...

        if snap.get("status") in TERMINAL_STATES:
            terminal = manager.get(job_id, include_result=True, include_events=True)
```

이벤트를 먼저 다 보내고 나서 terminal 체크를 합니다. 이것 자체는 의도된 순서일 수 있지만, **terminal 상태의 snap에서 이벤트를 보낸 뒤, 같은 job을 다시 `manager.get`으로 가져오면** 그 사이 새 이벤트가 추가될 수 있습니다 (다른 스레드에서). 심각한 버그는 아니지만 이벤트가 누락되거나 중복될 수 있는 race window가 있습니다.

**버그 3 (신규): follow-up 키워드 "energy level"이 일반 대화를 잘못 잡음**

```python
follow_up_keywords = ["homo", "lumo", "orbital", "esp", "charges", "dipole",
                       "energy level", "에너지", "오비탈", "전하"]
```

"energy level"은 두 단어인데 `in` 연산자로 substring 매칭합니다. "What is the energy level of benzene?"는 올바르게 follow-up으로 감지하지만, "explain energy levels in chemistry"처럼 분자를 언급하지 않는 일반 질문도 follow-up으로 감지합니다. 이 경우 `session_state["last_molecule"]`이 있으면 이전 분자에 대해 불필요한 계산이 트리거됩니다.

**버그 4 (신규): `_plan_status_message`의 반환값이 `|`로 구분됨**

```python
def _plan_status_message(plan, payload=None):
    # ...
    return " | ".join(parts)
```

이 함수의 반환값은 `chat.py`에서 assistant 메시지로 직접 전송됩니다:

```python
await _ws_send(
    websocket,
    "assistant",
    session_id=session_id,
    message=_plan_status_message(plan, prepared),
    # ...
)
```

프론트엔드 `chat.js`에서 에러 처리에는 파이프 처리가 있지만, assistant 메시지 처리에는 없습니다. 그래서 사용자에게 "Plan: analyze | structure=water | method=B3LYP | basis=def2-SVP | confidence=0.55" 같은 raw 내부 메시지가 그대로 보입니다. 사용자 친화적이지 않습니다.

---

### 파일 2: `compute.py`

**버그 5 (기존 식별): `list_jobs`에서 `list()` 이중 호출** — 이미 수정안 제시함.

**버그 6 (신규): `_heuristic_plan`에서 `_normalize_text_token` 후 정규식에 `re.IGNORECASE` 불필요**

```python
normalized = _normalize_text_token(text)  # 이미 .lower() 적용됨

if re.search(r"\b(homo|lumo|orbital|mo)\b|오비탈", normalized, re.IGNORECASE):
```

`_normalize_text_token`이 이미 `.lower()`를 적용하므로 `re.IGNORECASE`는 불필요합니다. 버그는 아니지만 코드 이해를 혼란스럽게 합니다.

**버그 7 (신규): `_fallback_extract_structure_query`의 noise 필터가 불완전**

```python
noise = ["homo", "lumo", "esp", "map", "orbital", "orbitals", "charge", "charges",
         "mulliken", "partial", "geometry", "optimization", "analysis", "of", "about",
         "for", "보여줘", "해줘", "계산"]
for n in noise:
    candidate_norm = re.sub(rf"\b{n}\b", " ", candidate_norm, flags=re.I)
```

문제: 한국어 단어("보여줘", "해줘", "계산")에 `\b`(word boundary)를 적용하고 있습니다. `\b`는 `\w`와 `\W` 사이의 경계인데, 한글은 `\w`에 포함되지 않는 로케일이 있습니다. Python의 `re` 모듈에서 기본적으로 `\w`는 유니코드 문자를 포함하므로 한글도 `\w`에 해당하지만, **`\b`가 한글 단어 경계를 제대로 잡지 못하는 경우**가 있습니다.

예: "벤젠보여줘"에서 `\b보여줘\b`는 매칭되지 않습니다. "보여줘"와 "벤젠" 사이에 word boundary가 없기 때문입니다 (둘 다 `\w` 문자). 결과적으로 "벤젠보여줘"가 입력되면 candidate가 "벤젠보여줘"가 되고, noise가 제거되지 않아서 "벤젠보여줘"를 분자 이름으로 인식하려고 시도합니다.

**버그 8 (신규): `_normalize_job_type`에서 기본 반환값이 항상 "analyze"**

```python
def _normalize_job_type(job_type, intent=None):
    jt = _normalize_text_token(job_type).replace(" ", "_")
    if jt in JOB_TYPE_ALIASES:
        return JOB_TYPE_ALIASES[jt]
    intent_key = _normalize_text_token(intent).replace(" ", "_")
    if intent_key in INTENT_TO_JOB_TYPE:
        return INTENT_TO_JOB_TYPE[intent_key]
    return "analyze"
```

`job_type`이 `None`이면 `_normalize_text_token(None)`은 빈 문자열을 반환하고, `"" in JOB_TYPE_ALIASES`는 `False`입니다. `intent`도 `None`이면 마찬가지. 그래서 **항상 "analyze"로 fallback**됩니다. 이는 사용자가 명시적으로 job_type을 지정했지만 alias에 없는 경우에도 조용히 "analyze"가 됩니다. 에러나 경고를 내야 할 상황을 삼킵니다.

**버그 9 (신규): `_normalize_result_contract`와 `_finalize_result_contract` 이중 정규화**

`_run_direct_compute`에서:

```python
def _run_direct_compute(payload, progress_callback=None):
    prepared = _prepare_payload(payload)
    # ...
    result = _invoke_callable_adaptive_sync(runner, prepared, progress_callback=progress_callback)
    return _normalize_result_contract(result, prepared)  # compute.py의 정규화
```

그런데 각 runner 함수(예: `run_analyze`)의 마지막 줄:

```python
return _finalize_result_contract(result)  # pyscf_runner.py의 정규화
```

두 함수 모두 `visualization.defaults`, `warnings`, `available` 등을 설정합니다. `_normalize_result_contract`는 `_finalize_result_contract`가 이미 설정한 `defaults.esp_preset`을 다시 설정하고, `available` 맵도 다시 계산합니다. **대부분 무해하지만**, `_normalize_result_contract`가 `payload`에서 읽는 값과 `_finalize_result_contract`가 `result`에서 읽는 값이 다를 수 있습니다.

구체적 예: `_normalize_result_contract`에서:

```python
out.setdefault("method", payload.get("method") or getattr(pyscf_runner, "DEFAULT_METHOD", "B3LYP"))
```

`setdefault`이므로 runner가 이미 설정한 method는 덮어쓰지 않습니다. 하지만:

```python
out.setdefault("structure_name", payload.get("structure_query") or payload.get("structure_name"))
```

만약 runner가 `structure_name`을 "water"로 설정했는데 payload에 `structure_query`가 "물"이었다면, `setdefault`이므로 "water"가 유지됩니다. 이건 올바른 동작이지만 **이 이중 경로를 통한 추론이 필요하다는 것 자체가 문제**입니다.

---

### 파일 3: `pyscf_runner.py`

**버그 10 (기존 식별): `_lookup_builtin_xyz` dead code 3중 복제** — 이미 수정안 제시함.

**버그 11 (기존 식별): `_run_scf_with_fallback` mol 재바인딩** — 이미 수정안 제시함.

**버그 12 (신규): `run_geometry_optimization`에서 `new_atom_text` 이중 계산**

```python
    method_name, mf = _build_mean_field(opt_mol, method or DEFAULT_METHOD)

    new_atom_text = _strip_xyz_header(_mol_to_xyz(opt_mol))  # 첫 번째 계산
    new_xyz = _mol_to_xyz(opt_mol, comment=structure_name or "QCViz-MCP")
    cache_key1 = _get_cache_key(new_xyz, method_name, basis or DEFAULT_BASIS, charge, multiplicity)

    mf, _ = _run_scf_with_fallback(mf, result["warnings"], cache_key=cache_key1, progress_callback=progress_callback)

    new_atom_text = _strip_xyz_header(_mol_to_xyz(opt_mol))  # 두 번째 계산 (동일!)
    final_result = _make_base_result(
        job_type="geometry_optimization",
        structure_name=structure_name,
        atom_text=new_atom_text,
```

`new_atom_text`가 두 번 계산됩니다. 동일한 `opt_mol`에서 동일한 결과가 나오므로 **버그는 아니지만 불필요한 중복**입니다.

**버그 13 (신규): `_extract_frontier_gap`에서 `best_lumo` 변수가 `best_homo`와 동일 객체를 가리킴**

```python
def _extract_frontier_gap(mf):
    # ...
    best_gap = None
    best_homo = None
    best_lumo = None

    for energies, occs, spin_label in zip(mo_energies, mo_occs, spin_labels):
        # ...
        if best_gap is None or gap_ha < best_gap:
            best_gap = gap_ha
            best_homo = info   # ← 같은 info 딕셔너리
            best_lumo = info   # ← 같은 info 딕셔너리
```

`best_homo`와 `best_lumo`가 **항상 같은 `info` 딕셔너리를 가리킵니다**. restricted 계산에서는 HOMO와 LUMO가 같은 채널에 있으므로 문제없지만, 이 코드의 의도는 best_homo와 best_lumo가 다른 채널일 수 있게 하려는 것처럼 보입니다. 실제로는:

```python
    if best_homo:
        out["homo_energy_hartree"] = best_homo["homo_energy_hartree"]
    if best_lumo:
        out["lumo_energy_hartree"] = best_lumo["lumo_energy_hartree"]
```

같은 딕셔너리에서 다른 키를 읽으므로 **결과는 올바릅니다**. 하지만 코드의 의도와 구조가 불일치합니다. unrestricted 다중채널에서 alpha HOMO와 beta LUMO의 gap이 가장 작은 경우를 올바르게 처리하지 못합니다.

**버그 14 (신규): `_compute_esp_auto_range`에서 density 마스킹 로직 버그**

```python
    if density_values is not None:
        dens = np.asarray(density_values, dtype=float).ravel()
        if dens.size == arr.size:
            dens = dens[np.isfinite(dens)]  # ← 크기가 줄어듦!
            if dens.size == arr.size:       # ← 줄어든 크기와 비교하므로 NaN이 있으면 실패
                low = density_iso * 0.35
                high = density_iso * 4.0
                mask = (density_values.ravel() >= low) & (density_values.ravel() <= high)
```

`dens = dens[np.isfinite(dens)]`가 NaN/Inf를 제거하면 `dens.size`가 `arr.size`보다 작아집니다. 그러면 바로 다음 `if dens.size == arr.size:` 조건이 False가 되어 마스킹이 **완전히 스킵**됩니다. density에 NaN이 하나라도 있으면 ESP 자동 범위 계산이 마스킹 없이 전체 값을 사용합니다.

의도된 동작은 NaN을 제거한 후에도 마스킹을 적용하는 것일 텐데, 현재 코드는 그렇지 않습니다.

수정:

```python
    if density_values is not None:
        dens_raw = np.asarray(density_values, dtype=float).ravel()
        if dens_raw.size == arr.size:
            finite_mask = np.isfinite(dens_raw) & np.isfinite(arr)
            if np.count_nonzero(finite_mask) >= 128:
                low = density_iso * 0.35
                high = density_iso * 4.0
                shell_mask = finite_mask & (dens_raw >= low) & (dens_raw <= high)
                if np.count_nonzero(shell_mask) >= 128:
                    masked = arr[shell_mask]
```

**버그 15 (신규): `_build_mean_field`에서 `wb97x-d` 중복 키**

```python
    xc_map = {
        "b3lyp": "b3lyp",
        "pbe": "pbe",
        "pbe0": "pbe0",
        "m06-2x": "m06-2x",
        "m062x": "m06-2x",
        "wb97x-d": "wb97x-d",
        "ωb97x-d": "wb97x-d",
        "wb97x-d": "wb97x-d",  # ← 중복 키!
    }
```

`"wb97x-d"`가 두 번 나옵니다. Python에서 중복 키는 마지막 값으로 덮어쓰므로 동작에 영향은 없지만, dead entry입니다. `"ωb97x-d"` 키를 넣으려 한 것으로 보이는데, `_normalize_name_token`에서 `ω → w`로 치환하므로 `"ωb97x-d"` 키는 절대 매칭되지 않습니다.

**버그 16 (신규): `_resolve_structure_payload`에서 PubChem 실패 시 에러 메시지가 내부적**

```python
    if structure_query:
        hit = _lookup_builtin_xyz(structure_query)
        if hit:
            # ...
        try:
            from qcviz_mcp.tools.core import MoleculeResolver
            resolved_xyz = MoleculeResolver.resolve_with_friendly_errors(structure_query)
        except Exception as e:
            pass  # ← 에러를 완전히 삼킴

    raise ValueError("No structure could be resolved; provide query, XYZ, or atom-spec text.")
```

PubChem/Molchat 해석이 실패하면 `except Exception as e: pass`로 에러를 완전히 삼킨 뒤, 제네릭한 "No structure could be resolved" 메시지를 던집니다. 사용자는 왜 실패했는지 알 수 없습니다. `MoleculeResolver.resolve_with_friendly_errors`가 이미 친화적 에러 메시지를 포함하므로, 이를 전파해야 합니다:

```python
        except Exception as e:
            raise ValueError(f"Structure resolution failed for '{structure_query}': {e}") from e
```

---

### 파일 4: `disk_cache.py`

**버그 17 (기존 식별): pickle 사용** — 이미 수정안 제시함.

---

### 파일 5: `tools/core.py`

**버그 18 (신규): `_sync_compute_ibo_impl`에서 Bohr→Å 변환 상수가 하드코딩**

```python
c = mol.atom_coord(i) * 0.529177249  # Bohr to Angstrom
```

`pyscf_runner.py`에는 `BOHR_TO_ANGSTROM = 0.529177210903`으로 정의되어 있는데, 여기서는 `0.529177249`를 사용합니다. 9번째 유효숫자에서 차이가 있습니다. 실용적으로 무시 가능하지만 상수가 일관적이지 않습니다. 또한 `pyscf_runner.py`에서는 `mol.atom_coords(unit="Angstrom")`를 사용하여 PySCF 내부 변환을 쓰는데, `core.py`에서는 수동 변환합니다.

**버그 19 (신규): `MoleculeResolver._resolve_name_to_smiles`에서 fallback 반환값이 `"O"`**

```python
        try:
            data = cls._http_get_json(direct_url, timeout=20)
            props = data.get("PropertyTable", {}).get("Properties", [])
            if props:
                p = props[0]
                return p.get("CanonicalSMILES") or p.get("IsomericSMILES") or p.get("SMILES") or p.get("ConnectivitySMILES") or "O"
```

PubChem이 프로퍼티를 반환했지만 모든 SMILES 필드가 None/빈 문자열인 경우, **`"O"` (물의 SMILES)**를 반환합니다. 사용자가 "caffeine"을 검색했는데 PubChem 응답이 비정상적이면 **조용히 물을 계산**합니다. 이것은 에러를 던져야 합니다.

수정:

```python
                smiles = p.get("CanonicalSMILES") or p.get("IsomericSMILES") or p.get("SMILES") or p.get("ConnectivitySMILES")
                if smiles:
                    return smiles
                # Fall through to CID-based lookup below
```

---

### 파일 6: `app.js`

**버그 20 (신규): `fetchHistory`에서 결과 데이터가 히스토리에 올바르게 병합되지 않음**

```javascript
function fetchHistory() {
    return fetch(PREFIX + "/compute/jobs?include_result=true")
      .then(function (res) {
        if (!res.ok) return;
        return res.json();
      })
      .then(function (data) {
        if (!data) return;
        var jobs = Array.isArray(data) ? data : (data.items || data.jobs || []);
        jobs.forEach(function (j) { App.upsertJob(j); });
      })
```

`include_result=true`로 요청하면 각 job snapshot에 `result`와 `error` 필드가 포함됩니다. `App.upsertJob`은 `deepMerge`를 사용하는데:

```javascript
upsertJob: function (job) {
    var prev = store.jobsById[jobId] || {};
    var next = deepMerge(prev, job);
    store.jobsById[jobId] = next;
    if (next.result) store.resultsByJobId[jobId] = clone(next.result);
```

`_snapshot`에서 `include_result=True`일 때만 `result`가 포함되고, 아닐 때는 `result` 키 자체가 없습니다. 문제는 **WebSocket에서 `job_submitted` 이벤트로 result 없는 snapshot을 먼저 upsert한 뒤**, 나중에 fetchHistory로 result 포함 snapshot을 upsert하면 `deepMerge`가 올바르게 result를 추가합니다. 이건 OK.

하지만 반대 순서(fetchHistory가 먼저 result 포함 job을 로드 → WebSocket이 status만 업데이트)에서는 문제가 없습니다. `deepMerge`가 기존 result를 보존하므로.

실제 문제는: `_snapshot`에서 `include_result=False`이면 snap에 `result` 키가 아예 없으므로, `deepMerge`에서 덮어쓸 일이 없습니다. **이건 올바른 동작입니다.** 재검토 결과 이 부분은 버그 아님.

---

### 파일 7: `chat.js`

**버그 21 (신규): `handleServerEvent`에서 `result` 이벤트 처리 시 `state.lastUserInput` 덮어쓰기**

```javascript
case "result":
    // ...
    if (result && rjid) {
        App.upsertJob({
            job_id: rjid, status: "completed", result: result,
            user_query: state.lastUserInput || (App.store.jobsById[rjid] ? App.store.jobsById[rjid].user_query : ""),
```

`state.lastUserInput`은 마지막 전송 메시지입니다. 만약 사용자가 job A를 실행하고, 결과를 기다리는 동안 job B를 제출하면, `state.lastUserInput`은 job B의 메시지입니다. job A의 결과가 도착하면 `user_query`에 job B의 메시지가 저장됩니다.

이를 방지하려면 job 제출 시점에 `lastUserInput`을 job ID와 매핑해서 저장해야 합니다. 현재 코드는 한 번에 하나의 job만 처리하는 것을 가정하고 있으므로 (WebSocket이 `_stream_backend_job_until_terminal`에서 블로킹), 실제로는 발생하지 않지만 구조적 취약점입니다.

**버그 22 (신규): `addProgressUI`의 step 개수 제한이 6개로 하드코딩**

```javascript
while (stepsEl.children.length > 6) {
  stepsEl.removeChild(stepsEl.firstChild);
}
```

SCF가 100 사이클 돌면서 2사이클마다 progress를 보내면 50개의 step이 추가되고 44개가 삭제됩니다. 이 과정에서 **매번 DOM 노드를 하나씩 제거**하므로 성능 이슈가 있을 수 있습니다. `DocumentFragment`나 batch removal이 좋겠지만 심각한 수준은 아닙니다.

---

### 파일 8: `viewer.js`

**버그 23 (기존 식별): `getXyz`에서 `xyz_block` 우선** — 이미 수정안 제시함.

**버그 24 (신규): orbital 선택기에서 `r.orbitals`를 사용하지만 `find` 메서드는 IE에서 미지원**

```javascript
var found = state.result.orbitals.find(function (o) {
  return o.zero_based_index === idx;
});
```

코드베이스의 나머지 부분은 `forEach`, `map` 등 ES5 메서드를 사용하는데, `find`는 ES6입니다. 최신 브라우저에서는 문제없지만 코드 일관성 관점에서 불일치.

**버그 25 (신규): `renderESP`에서 `esp_auto_range_au` 사용법이 비대칭**

```javascript
var range = result.esp_auto_range_au || 0.05;
viewer.addIsosurface(densVol, {
  isoval: state.isovalue,
  // ...
  volscheme: new window.$3Dmol.Gradient.RWB(-range, range),
});
```

여기서 `state.isovalue`는 **orbital isovalue** 슬라이더의 값(기본 0.03)입니다. ESP 맵에서 isosurface는 **electron density**의 isosurface인데, 이 값이 orbital isovalue와 같은 슬라이더를 공유합니다. ESP에서는 보통 0.001~0.01 범위가 적절하고, orbital에서는 0.02~0.08이 적절합니다. **모드가 바뀔 때 슬라이더 범위가 변경되지 않으므로**, orbital 모드에서 0.05로 설정한 후 ESP 모드로 전환하면 density isosurface가 0.05로 렌더링되어 **거의 아무것도 안 보일 수 있습니다**.

백엔드는 `visualization.defaults.esp_density_iso: 0.001`을 제공하지만, viewer.js는 이를 읽지 않습니다.

수정안:

```javascript
function renderESP(result) {
    return ensureViewer().then(function (viewer) {
        clearViewer(viewer);
        addMoleculeModel(viewer, result);

        var viz = result.visualization || {};
        var defaults = viz.defaults || {};

        /* ESP density iso는 별도 값 사용 */
        var espDensityIso = defaults.esp_density_iso || 0.001;

        var densB64 = viz.density_cube_b64 || result.density_cube_b64 || null;
        var espB64 = viz.esp_cube_b64 || result.esp_cube_b64 || null;

        try {
            if (densB64 && espB64) {
                var densVol = new window.$3Dmol.VolumeData(atob(densB64), "cube");
                var espVol = new window.$3Dmol.VolumeData(atob(espB64), "cube");
                var range = result.esp_auto_range_au || defaults.esp_range_au || 0.05;
                viewer.addIsosurface(densVol, {
                    isoval: espDensityIso,  // ← orbital isovalue 대신 ESP 전용 값
                    color: "white",
                    alpha: defaults.esp_opacity || state.opacity,
                    smoothness: 1,
                    voldata: espVol,
                    volscheme: new window.$3Dmol.Gradient.RWB(-range, range)
                });
            }
            // ...
```

---

### 파일 9: `style.css`

CSS에는 기능적 버그 없음. 다만:

**이슈 26 (신규): `.viewer-controls`의 `overflow-x: auto`와 `::-webkit-scrollbar { display: none }` 충돌**

```css
.viewer-controls {
  /* ... */
  overflow-x: auto;
}
.viewer-controls::-webkit-scrollbar {
  display: none;
}
```

스크롤바를 숨기면서 `overflow-x: auto`를 쓰면, 컨트롤이 넘칠 때 **스크롤이 가능하지만 사용자가 스크롤바를 볼 수 없어서 넘치는 컨트롤이 있다는 걸 모릅니다**. 모바일에서는 스와이프로 발견할 수 있지만 데스크톱에서는 문제. 이건 UX 이슈이지 버그는 아님.

---

### 파일 10: `index.html`

**이슈 27 (신규): 스크립트 로드 순서에 암묵적 의존성**

```html
<script src="/static/chat.js" defer></script>
<script src="/static/results.js" defer></script>
<script src="/static/viewer.js" defer></script>
<script src="/static/app.js" defer></script>
```

모든 스크립트가 `defer`이므로 HTML 파싱 순서대로 실행됩니다. `chat.js`가 먼저 실행되는데, `App.chat = { ... }`를 설정합니다. `viewer.js`에서 `App.chat.submit`을 호출합니다. `app.js`는 마지막에 실행되어 이벤트 리스너를 등록합니다.

문제: `app.js`에서 `App.on("jobs:changed", renderHistory)`를 등록하기 전에 `chat.js`가 이미 실행되어 WebSocket을 연결합니다. WebSocket이 매우 빨리 연결되어 `job_submitted` 이벤트가 오면, `App.upsertJob` → `emit("jobs:changed")` 시점에 `renderHistory` 리스너가 아직 등록되지 않았을 수 있습니다.

실제로는 WebSocket 연결 + 서버 처리 + 응답이 수 ms 이상 걸리므로 `app.js`가 먼저 로드됩니다. 하지만 **이론적 race condition**이 존재합니다.

---

### 파일 11: `job_manager.py`

**이슈 28 (기존 식별): 미사용 파일**

이 파일은 웹 라우트에서 import되지 않습니다. `compute.py`의 `InMemoryJobManager`가 대신 사용됩니다. 혼란의 원인이므로 삭제하거나, 하나로 통합해야 합니다.

---

## 전체 버그 목록 (총 28개)

| #   | 파일                         | 심각도  | 설명                                                      |
| --- | ---------------------------- | ------- | --------------------------------------------------------- |
| 1   | chat.py                      | 🔴 중   | 이벤트 중복 전송 (job_update + job_event)                 |
| 2   | chat.py                      | 🟡 낮   | terminal 상태 감지와 이벤트 전송 사이 race window         |
| 3   | chat.py                      | 🟡 낮   | "energy level" 키워드가 일반 질문을 follow-up으로 오인    |
| 4   | chat.py                      | 🟡 낮   | 내부 plan 메시지가 사용자에게 raw로 노출                  |
| 5   | compute.py                   | 🟡 낮   | `list_jobs`에서 `list()` 이중 호출                        |
| 6   | compute.py                   | ⚪ 미미 | `re.IGNORECASE` 불필요 (이미 lower된 문자열)              |
| 7   | compute.py                   | 🟡 낮   | 한국어 noise 단어에 `\b` word boundary 미작동             |
| 8   | compute.py                   | 🟡 낮   | 미인식 job_type이 조용히 "analyze"로 fallback             |
| 9   | compute.py + pyscf_runner.py | 🟡 낮   | 결과 정규화 이중 적용                                     |
| 10  | pyscf_runner.py              | 🔴 중   | `_lookup_builtin_xyz` dead code 3중 복제 (~60줄)          |
| 11  | pyscf_runner.py              | 🟡 낮   | `_run_scf_with_fallback` mol 재바인딩 방어 코드 불완전    |
| 12  | pyscf_runner.py              | ⚪ 미미 | `run_geometry_optimization`에서 `new_atom_text` 이중 계산 |
| 13  | pyscf_runner.py              | 🟡 낮   | `_extract_frontier_gap`에서 best_homo/best_lumo 동일 객체 |
| 14  | pyscf_runner.py              | 🔴 중   | `_compute_esp_auto_range` density 마스킹 NaN 처리 버그    |
| 15  | pyscf_runner.py              | ⚪ 미미 | `_build_mean_field` xc_map 중복 키                        |
| 16  | pyscf_runner.py              | 🔴 중   | `_resolve_structure_payload` PubChem 에러 삼킴            |
| 17  | disk_cache.py                | 🔴 중   | pickle deserialization 취약점                             |
| 18  | core.py                      | ⚪ 미미 | Bohr→Å 변환 상수 불일치                                   |
| 19  | core.py                      | 🔴 중   | PubChem 응답 비정상 시 `"O"` (물) 반환                    |
| 20  | —                            | —       | (재검토 결과 버그 아님)                                   |
| 21  | chat.js                      | 🟡 낮   | 동시 job 시 `lastUserInput` 교차 오염 (구조적)            |
| 22  | chat.js                      | ⚪ 미미 | progress step DOM 삭제 비효율                             |
| 23  | viewer.js                    | 🔴 중   | `getXyz`에서 `xyz_block` 우선 (백엔드는 `xyz` 사용)       |
| 24  | viewer.js                    | ⚪ 미미 | `Array.find` ES6 의존                                     |
| 25  | viewer.js                    | 🔴 중   | ESP 모드에서 orbital isovalue 슬라이더 공유               |
| 26  | style.css                    | ⚪ 미미 | 숨겨진 스크롤바로 overflow 미인지                         |
| 27  | index.html                   | ⚪ 미미 | 스크립트 로드 순서 암묵적 의존 (이론적 race)              |
| 28  | job_manager.py               | 🟡 낮   | 미사용 파일                                               |

그리고 **감사 보고서의 Cross-Check에서 놓친 가장 큰 문제**:

| #   | 심각도    | 설명                                                                                         |
| --- | --------- | -------------------------------------------------------------------------------------------- |
| ★   | 🔴🔴 치명 | `results.js`가 읽는 필드명 9개 이상이 백엔드와 불일치 (Summary/Charges/Orbital 탭 전부 깨짐) |

---

## 추가 수정 코드

이전 답변에서 이미 수정한 것: #1, #5, #10, #11, #17, #23, ★ (results.js 전면 재작성)

나머지 중요 수정:

### 버그 #14 수정: `_compute_esp_auto_range`

```python
def _compute_esp_auto_range(
    esp_values: np.ndarray,
    density_values: Optional[np.ndarray] = None,
    density_iso: float = 0.001,
) -> Dict[str, Any]:
    arr = np.asarray(esp_values, dtype=float).ravel()
    arr = arr[np.isfinite(arr)]
    if arr.size == 0:
        default_au = ESP_PRESETS_DATA["acs"]["default_range_au"]
        return {
            "range_au": default_au,
            "range_kcal": default_au * HARTREE_TO_KCAL,
            "stats": {},
            "strategy": "default",
        }

    masked = arr
    if density_values is not None:
        dens_raw = np.asarray(density_values, dtype=float).ravel()
        esp_raw = np.asarray(esp_values, dtype=float).ravel()
        if dens_raw.size == esp_raw.size:
            # finite mask on BOTH arrays simultaneously
            finite_mask = np.isfinite(dens_raw) & np.isfinite(esp_raw)
            if np.count_nonzero(finite_mask) >= 128:
                low = density_iso * 0.35
                high = density_iso * 4.0
                shell_mask = finite_mask & (dens_raw >= low) & (dens_raw <= high)
                if np.count_nonzero(shell_mask) >= 128:
                    masked = esp_raw[shell_mask]

    masked = masked[np.isfinite(masked)] if not np.all(np.isfinite(masked)) else masked
    if masked.size < 32:
        masked = arr

    abs_vals = np.abs(masked)
    p90 = float(np.percentile(abs_vals, 90))
    p95 = float(np.percentile(abs_vals, 95))
    p98 = float(np.percentile(abs_vals, 98))
    p995 = float(np.percentile(abs_vals, 99.5))
    robust = 0.55 * p95 + 0.35 * p98 + 0.10 * p995
    robust = float(np.clip(robust, 0.02, 0.18))
    nice = _nice_symmetric_limit(robust)

    return {
        "range_au": nice,
        "range_kcal": nice * HARTREE_TO_KCAL,
        "stats": {
            "n": int(masked.size),
            "min_au": float(np.min(masked)),
            "max_au": float(np.max(masked)),
            "mean_au": float(np.mean(masked)),
            "std_au": float(np.std(masked)),
            "p90_abs_au": p90,
            "p95_abs_au": p95,
            "p98_abs_au": p98,
            "p995_abs_au": p995,
        },
        "strategy": "robust_surface_shell_percentile",
    }
```

### 버그 #16 수정: PubChem 에러 전파

```python
def _resolve_structure_payload(
    structure_query: Optional[str] = None,
    xyz: Optional[str] = None,
    atom_spec: Optional[str] = None,
) -> Tuple[str, str]:
    if atom_spec and _safe_str(atom_spec):
        return _safe_str(structure_query, "custom"), _safe_str(atom_spec).strip()

    if xyz and _safe_str(xyz):
        atom_text = _strip_xyz_header(_safe_str(xyz))
        if atom_text:
            return _safe_str(structure_query, "custom"), atom_text

    if structure_query and _looks_like_xyz(structure_query):
        atom_text = _strip_xyz_header(_safe_str(structure_query))
        if atom_text:
            return "custom", atom_text

    if structure_query:
        hit = _lookup_builtin_xyz(structure_query)
        if hit:
            label, xyz_text = hit
            atom_text = _strip_xyz_header(xyz_text)
            return label, atom_text

        # Don't silently swallow the error
        resolve_error = None
        try:
            from qcviz_mcp.tools.core import MoleculeResolver
            resolved_xyz = MoleculeResolver.resolve_with_friendly_errors(structure_query)
            if resolved_xyz:
                atom_text = _strip_xyz_header(resolved_xyz)
                if atom_text:
                    return _safe_str(structure_query), atom_text
        except Exception as e:
            resolve_error = e

        if resolve_error:
            raise ValueError(
                f"Could not resolve structure '{structure_query}': {resolve_error}"
            ) from resolve_error

    raise ValueError("No structure could be resolved; provide query, XYZ, or atom-spec text.")
```

### 버그 #19 수정: `"O"` fallback 제거

```python
    @classmethod
    def _resolve_name_to_smiles(cls, name: str) -> str:
        quoted = urllib.parse.quote(name.strip(), safe="")
        direct_url = (
            f"{cls.PUBCHEM_BASE}/compound/name/{quoted}/property/CanonicalSMILES,IsomericSMILES/JSON"
        )

        try:
            data = cls._http_get_json(direct_url, timeout=20)
            props = data.get("PropertyTable", {}).get("Properties", [])
            if props:
                p = props[0]
                smiles = (p.get("CanonicalSMILES") or p.get("IsomericSMILES")
                          or p.get("SMILES") or p.get("ConnectivitySMILES"))
                if smiles:
                    return smiles
                # If PubChem returned properties but no SMILES, fall through to CID lookup
        except urllib.error.HTTPError as e:
            logger.warning("PubChem direct name->SMILES failed for %s: %s", name, e)
        except Exception as e:
            logger.warning("PubChem direct name->SMILES error for %s: %s", name, e)

        # CID-based lookup (unchanged)
        cid_url = f"{cls.PUBCHEM_BASE}/compound/name/{quoted}/cids/JSON"
        # ... rest unchanged ...
```

### 버그 #25 수정: ESP isovalue 분리

`viewer.js`의 `renderESP` 함수만 교체 (이전 답변의 수정안에 포함됨). 추가로 isovalue 슬라이더의 범위를 모드에 따라 변경:

```javascript
function showControls(mode) {
  if ($empty) $empty.hidden = true;
  if ($controls) $controls.hidden = false;
  if ($grpOrbital) $grpOrbital.hidden = mode !== "orbital" && mode !== "esp";
  if ($grpOpacity) $grpOpacity.hidden = mode !== "orbital" && mode !== "esp";

  /* Adjust slider range based on mode */
  if ($sliderIso) {
    if (mode === "esp") {
      $sliderIso.min = "0.0001";
      $sliderIso.max = "0.01";
      $sliderIso.step = "0.0001";
      if (state.isovalue > 0.01) {
        state.isovalue = 0.002;
        $sliderIso.value = state.isovalue;
      }
    } else {
      $sliderIso.min = "0.001";
      $sliderIso.max = "0.1";
      $sliderIso.step = "0.001";
      if (state.isovalue < 0.001) {
        state.isovalue = 0.03;
        $sliderIso.value = state.isovalue;
      }
    }
    if ($lblIso) $lblIso.textContent = state.isovalue.toFixed(4);
  }

  var hasOrb =
    state.result && state.result.orbitals && state.result.orbitals.length > 0;
  if (!hasOrb) {
    hasOrb =
      state.result &&
      state.result.mo_energies &&
      state.result.mo_energies.length > 0;
  }
  if ($grpOrbitalSelect)
    $grpOrbitalSelect.hidden = mode !== "orbital" || !hasOrb;
}
```

### 버그 #12 수정: `run_geometry_optimization` 중복 제거

```python
    mf, _ = _run_scf_with_fallback(mf, result["warnings"], cache_key=cache_key1, progress_callback=progress_callback)

    # new_atom_text already computed above, no need to recalculate
    final_result = _make_base_result(
        job_type="geometry_optimization",
        structure_name=structure_name,
        atom_text=new_atom_text,  # ← uses the one computed before SCF
```

두 번째 `new_atom_text = _strip_xyz_header(_mol_to_xyz(opt_mol))` 줄을 삭제하면 됩니다.

---

## 최종 감사 보고서 판정

**원 보고서 신뢰도: 약 50%.**

12개 CHECK 항목 중 완전히 정확한 것은 5개(#1, #4, #5, #8, #12). 나머지는 버그를 놓쳤거나 코드 인용이 부정확합니다. Cross-Check의 "완벽히 맞춰졌습니다"는 results.js의 9개 이상 필드 불일치를 완전히 놓친 **치명적 오진**입니다.
