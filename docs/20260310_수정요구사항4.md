4차 전수검사를 진행합니다. 이전 34개 버그를 모두 인지한 상태에서, 아직 놓친 것이 있는지 각 파일의 모든 함수를 라인 단위로 재검토합니다.

---

## 4차 전수검사 — 신규 발견만

### `chat.py`

**버그 44 (신규): `websocket_chat`에서 `HTTPException` 처리 후 `continue`하지만 `prepared` 변수가 아직 정의 안 됨**

```python
            try:
                prepared = _prepare_payload(merged)
            except HTTPException as exc:
                # ... send error ...
                continue

            if prepared.get("structure_query") or prepared.get("structure_name"):
                session_state["last_molecule"] = prepared.get("structure_query") or ...
```

`_prepare_payload`가 예외를 던지면 `continue`로 루프 처음으로 돌아가므로, 아래의 `prepared.get(...)` 코드에 도달하지 않습니다. **이건 버그 아님.** `continue`가 정확히 올바르게 동작.

**버그 45 (신규): `_result_summary`에서 `energy`가 문자열일 수 있음**

```python
def _result_summary(result):
    energy = result.get("total_energy_hartree")
    # ...
    if energy is not None:
        try:
            parts.append(f"E={float(energy):.8f} Ha")
        except Exception:
            pass
```

`float()` 변환에 `try/except`가 있으므로 안전합니다. **버그 아님.**

**4차에서 chat.py 신규 발견: 없음.**

---

### `compute.py`

**버그 46 (신규): `_heuristic_plan`에서 반환하는 `job_type`이 payload의 기존 `job_type`을 무시할 수 있음**

```python
def _heuristic_plan(message, payload=None):
    payload = payload or {}
    # ...
    job_type = _normalize_job_type(payload.get("job_type"), intent)
    return {
        # ...
        "job_type": job_type,
    }
```

`_merge_plan_into_payload`에서:

```python
if not out.get("job_type"):
    out["job_type"] = _normalize_job_type(plan.get("job_type"), intent)
```

payload에 이미 `job_type`이 있으면 plan의 것으로 덮어쓰지 않습니다. `_heuristic_plan` 내부에서도 `payload.get("job_type")`을 `_normalize_job_type`에 전달하므로, payload의 job_type이 우선됩니다. **이건 올바른 동작.**

**버그 47 (신규): `InMemoryJobManager._run_job`에서 `progress_callback`이 job 딕셔너리 외부에서 `job_id`를 클로저로 캡처**

```python
def _run_job(self, job_id: str) -> None:
    with self.lock:
        job = self.jobs[job_id]
        job.status = "running"
        # ...

    def progress_callback(*args, **kwargs):
        # ...
        with self.lock:
            record = self.jobs[job_id]  # ← 클로저로 job_id 캡처
```

이건 의도된 동작이고, `job_id`는 문자열이므로 변경 불가능합니다. **버그 아님.**

**버그 48 (신규): `_prepare_payload`가 `_safe_plan_message`를 호출하고, `_safe_plan_message`가 `_heuristic_plan`을 호출하고, `_heuristic_plan`이 `_fallback_extract_structure_query`를 호출하는데, `_merge_plan_into_payload`에서도 `_fallback_extract_structure_query`를 다시 호출**

```python
def _prepare_payload(payload):
    data = dict(payload or {})
    raw_message = _extract_message(data)

    if raw_message and not data.get("planner_applied"):
        plan = _safe_plan_message(raw_message, data)       # ← 내부에서 _fallback_extract_structure_query 호출
        data = _merge_plan_into_payload(data, plan, ...)    # ← 또 _fallback_extract_structure_query 호출

    # ...
    if not data.get("structure_query") and not data.get("xyz") and not data.get("atom_spec"):
        fallback = _fallback_extract_structure_query(raw_message)  # ← 세 번째 호출
```

`_fallback_extract_structure_query`가 **최대 3번** 호출됩니다. 이 함수는 정규식을 여러 개 실행하고 라이브러리를 순회하므로 비용이 있습니다. 버그는 아니지만 **불필요한 중복 연산**입니다.

성능 이슈 수준이고, 한 번의 결과를 재사용하면 됩니다. 심각도: ⚪ 미미.

**4차에서 compute.py 실질적 신규 발견: 없음** (48은 성능 이슈).

---

### `pyscf_runner.py`

**버그 49 (신규): `_extract_mulliken_charges`에서 `s = getattr(mf, 'get_ovlp', lambda: active_mol.intor_symmetric("int1e_ovlp"))()`의 lambda 타이밍 문제**

```python
s = getattr(mf, 'get_ovlp', lambda: active_mol.intor_symmetric("int1e_ovlp"))()
```

`getattr(mf, 'get_ovlp', <default>)`에서:

- `mf`에 `get_ovlp` 메서드가 있으면 → `mf.get_ovlp()`를 호출
- 없으면 → `lambda: active_mol.intor_symmetric("int1e_ovlp")`를 호출

문제: `mf.get_ovlp`이 메서드가 아니라 None이 할당된 속성이면, `getattr`은 None을 반환하고 `None()`으로 호출하여 `TypeError`가 발생합니다. PySCF의 SCF 객체는 항상 callable `get_ovlp`을 가지므로 실제로 발생하지 않지만, disk cache에서 복원한 mf 객체에는 `get_ovlp` 메서드가 복원되지 않을 수 있습니다.

disk cache의 `load_from_disk`에서 `mo_energy`, `mo_occ`, `mo_coeff`, `converged`, `e_tot`만 복원합니다. `get_ovlp`은 **메서드**이므로 클래스에서 상속됩니다. `mf_obj`는 원래 `_build_mean_field`에서 생성한 RKS/RHF 객체이고, `load_from_disk`는 이 객체의 속성만 덮어쓰므로 메서드는 보존됩니다. **따라서 버그 아님.**

**버그 50 (신규): `_extract_lowdin_charges`에서 `from pyscf.scf import hf as scf_hf` 반복 import**

```python
def _extract_lowdin_charges(mol, mf):
    try:
        # ...
        from pyscf.scf import hf as scf_hf
        try:
            _, chg = scf_hf.lowdin_pop(active_mol, dm, s=s, verbose=0)
```

`_extract_mulliken_charges`에서도:

```python
        except AttributeError:
            from pyscf.scf import hf as scf_hf
            _, chg = scf_hf.mulliken_pop(active_mol, dm, s=s, verbose=0)
```

Python의 import 시스템은 이미 로드된 모듈을 캐싱하므로 (`sys.modules`), 반복 import는 성능에 거의 영향 없습니다. **버그 아님.** 코드 스타일 이슈.

**버그 51 (신규): `_restricted_or_unrestricted_arrays`에서 `list` 타입 체크가 불완전**

```python
def _restricted_or_unrestricted_arrays(mf):
    mo_energy = mf.mo_energy
    mo_occ = mf.mo_occ
    mo_coeff = mf.mo_coeff
    if isinstance(mo_energy, tuple):
        return list(mo_energy), list(mo_occ), list(mo_coeff), ["alpha", "beta"]
    if isinstance(mo_energy, list) and mo_energy and isinstance(mo_energy[0], np.ndarray):
        return list(mo_energy), list(mo_occ), list(mo_coeff), ["alpha", "beta"][: len(mo_energy)]
    return [np.asarray(mo_energy)], [np.asarray(mo_occ)], [np.asarray(mo_coeff)], ["restricted"]
```

UKS/UHF에서 `mo_energy`는 shape `(2, nmo)`의 ndarray일 수 있습니다 (tuple이 아님). 이 경우:

- `isinstance(mo_energy, tuple)` → False
- `isinstance(mo_energy, list)` → False
- 마지막 줄: `np.asarray(mo_energy)` → shape (2, nmo) → `[np.asarray(mo_energy)]` → 리스트 안에 2D 배열

이후 `_build_orbital_items`에서:

```python
for ch, (energies, occs, spin_label) in enumerate(zip(mo_energies, mo_occs, spin_labels)):
    energies = np.asarray(energies, dtype=float)
```

`mo_energies = [np.array([[...], [...]])]`이고 `spin_labels = ["restricted"]`이므로, `ch=0`, `energies`는 shape (2, nmo). `occ_idx = np.where(occs > 1e-8)[0]`에서 `occs`도 shape (2, nmo)이므로 `np.where`가 2D 인덱스를 반환하고 `[0]`으로 첫 번째 차원 인덱스만 가져옵니다.

이건 **unrestricted 계산에서 orbital 분석이 잘못될 수 있는 진짜 버그**입니다.

PySCF에서 UKS/UHF의 `mo_energy`는 보통 `numpy.ndarray` with shape `(2, nmo)`입니다. 이를 올바르게 처리하려면:

```python
def _restricted_or_unrestricted_arrays(mf):
    mo_energy = mf.mo_energy
    mo_occ = mf.mo_occ
    mo_coeff = mf.mo_coeff

    if isinstance(mo_energy, tuple):
        return list(mo_energy), list(mo_occ), list(mo_coeff), ["alpha", "beta"]

    if isinstance(mo_energy, list) and mo_energy and isinstance(mo_energy[0], np.ndarray):
        return list(mo_energy), list(mo_occ), list(mo_coeff), ["alpha", "beta"][:len(mo_energy)]

    mo_energy = np.asarray(mo_energy)
    mo_occ = np.asarray(mo_occ)
    mo_coeff = np.asarray(mo_coeff)

    # Check if unrestricted: 2D energy array with shape (2, nmo)
    if mo_energy.ndim == 2 and mo_energy.shape[0] == 2:
        return (
            [mo_energy[0], mo_energy[1]],
            [mo_occ[0], mo_occ[1]],
            [mo_coeff[0], mo_coeff[1]] if mo_coeff.ndim == 3 else [mo_coeff, mo_coeff],
            ["alpha", "beta"],
        )

    return [mo_energy], [mo_occ], [mo_coeff], ["restricted"]
```

이건 `_build_mean_field`에서 `is_open_shell`이 True일 때 UHF/UKS를 사용하므로, spin이 0이 아닌 분자에서 발생합니다. 예: O2 (triplet), NO (doublet).

**심각도: 🔴 중. unrestricted 계산에서 orbital 분석 전체가 깨짐.**

**버그 52 (신규): `_coalesce_density_matrix`에서 tuple/3D 처리는 하지만, UHF의 dm이 list of ndarray일 수 있음**

```python
def _coalesce_density_matrix(dm):
    if isinstance(dm, tuple):
        return np.asarray(dm[0]) + np.asarray(dm[1])
    dm_arr = np.asarray(dm)
    if dm_arr.ndim == 3 and dm_arr.shape[0] == 2:
        return dm_arr[0] + dm_arr[1]
    return dm_arr
```

PySCF의 UHF에서 `mf.make_rdm1()`은 `(dm_alpha, dm_beta)` 튜플 또는 shape `(2, nao, nao)` ndarray를 반환합니다. list인 경우도 있습니다:

```python
if isinstance(dm, list):
    return np.asarray(dm[0]) + np.asarray(dm[1])
```

이 줄이 빠져있지만, `np.asarray(dm)` 호출에서 list of ndarray → 3D ndarray로 변환되므로 `dm_arr.ndim == 3` 조건에 걸립니다. **따라서 실제로는 동작함.** 하지만 list의 원소가 크기가 다르면 `np.asarray`가 object array를 만들고 `ndim`이 1이 되어 잘못됩니다. PySCF에서는 항상 같은 크기이므로 **현실적으로 버그 아님.**

**버그 53 (신규): `run_geometry_optimization`에서 최적화 후 final SCF가 캐시를 사용하면 최적화된 좌표와 무관한 데이터가 반환될 수 있음**

```python
    opt_mol = geometric_optimize(mf0)  # 최적화된 geometry

    method_name, mf = _build_mean_field(opt_mol, method or DEFAULT_METHOD)

    new_atom_text = _strip_xyz_header(_mol_to_xyz(opt_mol))
    new_xyz = _mol_to_xyz(opt_mol, comment=structure_name or "QCViz-MCP")
    cache_key1 = _get_cache_key(new_xyz, method_name, basis or DEFAULT_BASIS, charge, multiplicity)

    mf, _ = _run_scf_with_fallback(mf, result["warnings"], cache_key=cache_key1, ...)
```

`cache_key1`은 **최적화된 좌표**로 생성됩니다. 캐시에 이 키가 있으면 이전에 같은 최적화된 좌표로 SCF를 수행한 적이 있다는 뜻이므로, 캐시 히트가 올바릅니다.

하지만 문제는: 최적화된 좌표의 부동소수점 표현이 매번 미세하게 다를 수 있다는 점 (버그 7에서 언급한 cache key 불안정성). `geometric_optimize`가 동일한 초기 구조에서 시작해도, 수렴 조건과 floating point 연산의 비결정성으로 인해 마지막 자리가 다를 수 있습니다. 이 경우 캐시 미스가 발생하여 SCF를 다시 수행하므로 **정확도에는 문제없지만 캐시 효율이 떨어짐**.

**이건 이전에 식별한 캐시키 불안정성의 구체적 케이스. 새 버그 아님.**

**버그 54 (신규): `_guess_bonds`에서 self-bond 체크가 없음**

```python
def _guess_bonds(mol):
    coords = np.asarray(mol.atom_coords(unit="Angstrom"), dtype=float)
    symbols = [mol.atom_symbol(i) for i in range(mol.natm)]
    bonds = []
    for i in range(mol.natm):
        for j in range(i + 1, mol.natm):  # i < j이므로 self-bond 불가
```

`range(i + 1, mol.natm)`이므로 `i == j`는 불가능합니다. **버그 아님.**

**버그 55 (신규): `_build_mol`에서 `spin`이 음수일 수 있음**

```python
def _build_mol(atom_text, basis=None, charge=0, multiplicity=1, unit="Angstrom"):
    basis_name = _normalize_basis_name(basis or DEFAULT_BASIS)
    spin = max(int(multiplicity or 1) - 1, 0)
```

`max(..., 0)`으로 음수를 방지합니다. `multiplicity`가 0이면 `int(0) - 1 = -1`, `max(-1, 0) = 0`. `multiplicity`가 None이면 `int(1) - 1 = 0`. **안전함.**

하지만 `multiplicity`가 문자열 "abc"이면? `_prepare_payload`에서:

```python
data["multiplicity"] = _safe_int(data.get("multiplicity"), 1) or 1
```

`_safe_int("abc", 1)`은 `1`을 반환합니다. **안전함.**

**버그 56 (신규): `_emit_progress`에서 첫 번째 try가 성공하면 return하지만, 딕셔너리가 아닌 경우 두 번째 호출 시도**

```python
def _emit_progress(progress_callback, progress, step, message=None, **extra):
    if not callable(progress_callback):
        return

    payload = {
        "progress": float(progress),
        "step": _safe_str(step, "working"),
        "message": _safe_str(message, message or step),
    }
    payload.update(extra)

    try:
        progress_callback(payload)   # 딕셔너리 하나로 호출
        return
    except TypeError:
        pass
    except Exception:
        return

    try:
        progress_callback(float(progress), _safe_str(step, "working"), payload["message"])  # 3개 인자로 호출
    except Exception:
        return
```

첫 번째 `try`에서 `TypeError`가 발생하면 (callback이 딕셔너리를 받지 않는 경우), 두 번째 `try`에서 3개 인자로 재시도합니다. 이건 `InMemoryJobManager`의 `progress_callback`과 `job_manager.py`의 `progress_callback`의 시그니처 차이를 흡수하기 위한 것입니다.

`InMemoryJobManager.progress_callback`:

```python
def progress_callback(*args, **kwargs):
    payload = {}
    if args and isinstance(args[0], Mapping):
        payload.update(dict(args[0]))
    else:
        if len(args) >= 1: payload["progress"] = args[0]
        # ...
```

이건 딕셔너리와 positional args 모두 받으므로 첫 번째 `try`에서 성공합니다. **버그 아님.** 방어적 코드.

**4차에서 pyscf_runner.py 신규 발견: #51 (UHF/UKS ndarray 처리).**

---

### `disk_cache.py` 재검토

**버그 57 (신규): `save_to_disk`에서 `mf_obj.converged`가 numpy bool일 수 있음**

```python
if hasattr(mf_obj, 'converged'): fh5['scf/converged'] = mf_obj.converged
```

`mf_obj.converged`는 Python bool 또는 numpy bool일 수 있습니다. h5py는 numpy bool을 직접 저장할 수 있으므로 **문제없음.**

`load_from_disk`에서:

```python
mf_obj.converged = bool(fh5['scf/converged'][()])
```

`bool()`로 변환하므로 안전합니다. **버그 아님.**

---

### `core.py` 재검토

**버그 58 (신규): `_resolve_query_input`에서 `validate_atom_spec_strict`가 XYZ 형식을 거부할 수 있음**

```python
def _resolve_query_input(query):
    resolved_structure = MoleculeResolver.resolve_with_friendly_errors(query)
    validate_atom_spec_strict(resolved_structure)  # ← XYZ 형식(헤더 포함)을 검증
    atom_data = _parse_atom_spec(resolved_structure)
```

`MoleculeResolver.resolve`는 XYZ 형식(원자수 + 코멘트 + 좌표)을 반환합니다. `validate_atom_spec_strict`가 어떻게 구현되어 있는지는 `security.py`에 있는데 코드가 제공되지 않았습니다. 만약 이 함수가 순수 atom-spec (좌표만)을 기대한다면, XYZ 헤더(원자수, 코멘트 줄)가 검증 실패를 유발할 수 있습니다.

`_parse_atom_spec`은 헤더를 제거하지만, `validate_atom_spec_strict`는 **그 전에** 호출됩니다. 이건 **잠재적 버그**이지만, `security.py`의 구현을 볼 수 없어 확정할 수 없습니다.

**심각도: 🟡 낮 (불확실).**

**버그 59 (신규): `compute_ibo`에서 `cache_key`에 `display_name_hint`가 포함됨**

```python
cache_key = cache.make_key(
    "compute_ibo",
    resolved_structure=resolved_structure,
    display_name_hint=display_name_hint,  # ← 표시 이름이 캐시 키에 영향
    basis=basis,
    # ...
)
```

같은 분자라도 "water"로 검색한 것과 "H2O"로 검색한 것의 `display_name_hint`가 다르므로 **캐시 미스**가 발생합니다. `resolved_structure`(XYZ 좌표)가 같더라도 `display_name_hint`가 다르면 캐시를 재사용하지 못합니다.

**심각도: 🟡 낮.** 정확도에는 영향 없지만 캐시 효율이 떨어짐.

---

### `app.js` 재검토

**버그 60 (신규): `deepMerge`에서 배열이 병합되지 않고 교체됨**

```javascript
function deepMerge(base, patch) {
  var lhs = base && typeof base === "object" ? clone(base) : {};
  var rhs = patch && typeof patch === "object" ? patch : {};
  Object.keys(rhs).forEach(function (k) {
    var lv = lhs[k],
      rv = rhs[k];
    if (
      lv &&
      rv &&
      typeof lv === "object" &&
      typeof rv === "object" &&
      !Array.isArray(lv) &&
      !Array.isArray(rv)
    ) {
      lhs[k] = deepMerge(lv, rv);
    } else {
      lhs[k] = clone(rv);
    }
  });
  return lhs;
}
```

배열인 경우 `!Array.isArray(lv) && !Array.isArray(rv)`에서 `false`가 되어 **배열은 항상 교체**됩니다. 이는 `warnings` 배열이 있는 job을 업데이트할 때, 새 snapshot에 `warnings`가 빈 배열이면 기존 warnings가 사라집니다.

실제로: `_snapshot`에서 `include_result=False`이면 `warnings`가 snap에 포함되지 않으므로 덮어쓰지 않습니다. `include_result=True`이면 최신 전체 result를 가져오므로 교체가 올바릅니다. **현재 사용 패턴에서는 문제없음.**

하지만 `atoms`, `bonds`, `mulliken_charges` 같은 배열 필드도 마찬가지로 교체됩니다. 이건 의도된 동작입니다 (부분 업데이트가 아니라 전체 교체). **버그 아님.**

**버그 61 (신규): `upsertJob`에서 `jobOrder` 정렬이 매 upsert마다 O(n log n)**

```javascript
store.jobOrder = Object.values(store.jobsById)
  .sort(function (a, b) {
    return Number(b.updated_at || 0) - Number(a.updated_at || 0);
  })
  .map(function (j) {
    return j.job_id;
  });
```

job이 200개이면 매번 200개를 정렬합니다. progress 이벤트가 초당 수회 올 수 있으므로 (SCF 2사이클마다), 초당 수회 × 200개 정렬이 발생합니다. **현실적으로는 200개 정도는 무시 가능하지만**, MAX_JOBS가 늘어나면 문제.

**심각도: ⚪ 미미.**

---

### `chat.js` 재검토

**버그 62 (신규): `handleServerEvent`의 default case에서 무한 재귀 가능성**

```javascript
default:
    if (msg.result || msg.results || (msg.data && msg.data.total_energy_hartree)) {
        handleServerEvent(Object.assign({}, msg, { type: "result" })); return;
    }
    if (status === "completed" || ...) {
        handleServerEvent(Object.assign({}, msg, { type: "result" })); return;
    }
    // ...
```

`Object.assign({}, msg, { type: "result" })`로 새 객체를 만들어 재귀 호출합니다. `type: "result"`를 설정하므로 switch의 `case "result":`에 걸립니다. 이 case에서 다시 default로 빠질 수는 없으므로 **무한 재귀는 발생하지 않습니다.**

하지만 만약 `case "result":` 안에서 `handleServerEvent`를 다시 호출하는 경로가 있다면?

```javascript
case "result":
    // ... 처리 ...
    break;
```

`break`로 끝나므로 재귀 없음. **버그 아님.**

**버그 63 (신규): WebSocket 재연결 시 이전 연결의 이벤트 핸들러가 남아있을 수 있음**

```javascript
function connectWS() {
    if (state.ws && (state.ws.readyState === WebSocket.OPEN || state.ws.readyState === WebSocket.CONNECTING)) return;
    try {
        state.ws = new WebSocket(buildWsUrl());
    } catch (e) {
        // ...
    }
    state.ws.onopen = function () { ... };
    state.ws.onclose = function () { ... };
    state.ws.onmessage = function () { ... };
}
```

`state.ws`에 새 WebSocket을 할당하기 전에, 이전 WebSocket의 `onclose`가 아직 발화하지 않았을 수 있습니다. 이전 WebSocket이 CLOSING 상태일 때:

- `state.ws.readyState`가 `CLOSING`(2)이므로 guard를 통과
- `state.ws = new WebSocket(...)` → 새 WS 할당
- 이전 WS의 `onclose` 발화 → `setWsUI(false)` + `scheduleReconnect()`
- 새 WS의 `onopen` 발화 → `setWsUI(true)`

`onclose`와 `onopen`의 타이밍에 따라 UI가 잠시 Disconnected로 깜빡일 수 있습니다. 또한 `scheduleReconnect()`가 불필요하게 호출되어 **또 다른 WebSocket 연결을 시도**할 수 있습니다.

수정: 이전 WS의 핸들러를 제거하거나, reconnect에서 현재 WS의 상태를 확인:

```javascript
function connectWS() {
  if (state.ws) {
    if (
      state.ws.readyState === WebSocket.OPEN ||
      state.ws.readyState === WebSocket.CONNECTING
    )
      return;
    // Clear handlers on old WS to prevent ghost callbacks
    state.ws.onopen = null;
    state.ws.onclose = null;
    state.ws.onerror = null;
    state.ws.onmessage = null;
  }
  // ... create new WS ...
}
```

**심각도: 🟡 낮.** 실제로는 거의 발생하지 않지만 edge case.

---

### `results.js` (이전 수정안) 재검토

이전 수정안에서 `normalizeResult`의 orbitals → mo_energies 변환을 검토:

```javascript
if (
  (!r.mo_energies || !r.mo_energies.length) &&
  r.orbitals &&
  r.orbitals.length
) {
  var sorted = r.orbitals.slice().sort(function (a, b) {
    return a.zero_based_index - b.zero_based_index;
  });
  r.mo_energies = sorted.map(function (o) {
    return o.energy_hartree;
  });
  r.mo_occupations = sorted.map(function (o) {
    return o.occupancy;
  });
  r._orbital_index_offset = sorted[0] ? sorted[0].zero_based_index : 0;
  r._orbital_labels = sorted.map(function (o) {
    return o.label;
  });
}
```

이건 올바릅니다. `orbitals` 배열은 window 범위 내의 orbital만 포함하므로, `mo_energies`도 해당 범위만 포함합니다. `_orbital_index_offset`으로 실제 인덱스를 추적합니다.

---

### `viewer.js` (이전 수정안) 재검토

`getXyz` 수정안:

```javascript
var xyz =
  viz.xyz ||
  viz.molecule_xyz ||
  viz.xyz_block ||
  result.xyz_block ||
  result.xyz ||
  null;
```

이건 올바릅니다. 백엔드가 `viz.xyz`와 `viz.molecule_xyz`를 모두 설정하므로 첫 번째에서 매칭됩니다.

---

## 4차 검사 최종 신규 발견

| #   | 파일            | 심각도  | 설명                                                            |
| --- | --------------- | ------- | --------------------------------------------------------------- |
| 48  | compute.py      | ⚪ 미미 | `_fallback_extract_structure_query` 최대 3회 중복 호출          |
| 51  | pyscf_runner.py | 🔴 중   | UHF/UKS에서 mo_energy가 2D ndarray일 때 orbital 분석 오류       |
| 58  | core.py         | 🟡 낮   | `validate_atom_spec_strict`가 XYZ 헤더를 거부할 가능성 (불확실) |
| 59  | core.py         | 🟡 낮   | 캐시 키에 display_name_hint 포함으로 캐시 효율 저하             |
| 61  | app.js          | ⚪ 미미 | 매 upsert마다 전체 jobOrder 재정렬                              |
| 63  | chat.js         | 🟡 낮   | WS 재연결 시 이전 핸들러가 ghost callback 유발 가능             |

---

## 수정 코드

### 버그 #51 수정: `_restricted_or_unrestricted_arrays`

```python
def _restricted_or_unrestricted_arrays(mf):
    mo_energy = mf.mo_energy
    mo_occ = mf.mo_occ
    mo_coeff = mf.mo_coeff

    # Case 1: tuple (e.g., from some UHF implementations)
    if isinstance(mo_energy, tuple):
        labels = ["alpha", "beta"][:len(mo_energy)]
        return list(mo_energy), list(mo_occ), list(mo_coeff), labels

    # Case 2: list of arrays
    if isinstance(mo_energy, list) and mo_energy and isinstance(mo_energy[0], np.ndarray):
        labels = ["alpha", "beta"][:len(mo_energy)]
        return list(mo_energy), list(mo_occ), list(mo_coeff), labels

    # Case 3: numpy arrays — check dimensionality
    mo_energy = np.asarray(mo_energy)
    mo_occ = np.asarray(mo_occ)

    if mo_energy.ndim == 2 and mo_energy.shape[0] == 2:
        # Unrestricted: shape (2, nmo)
        mo_coeff_arr = np.asarray(mo_coeff)
        if mo_coeff_arr.ndim == 3 and mo_coeff_arr.shape[0] == 2:
            coeff_list = [mo_coeff_arr[0], mo_coeff_arr[1]]
        elif isinstance(mo_coeff, (tuple, list)) and len(mo_coeff) == 2:
            coeff_list = [np.asarray(mo_coeff[0]), np.asarray(mo_coeff[1])]
        else:
            # Fallback: use same coeff for both channels (shouldn't happen)
            coeff_list = [mo_coeff_arr, mo_coeff_arr]
        return [mo_energy[0], mo_energy[1]], [mo_occ[0], mo_occ[1]], coeff_list, ["alpha", "beta"]

    # Case 4: restricted (1D arrays)
    mo_coeff = np.asarray(mo_coeff)
    return [mo_energy], [mo_occ], [mo_coeff], ["restricted"]
```

### 버그 #63 수정: WebSocket 재연결 시 ghost handler 방지

```javascript
function connectWS() {
  // Clear old handlers to prevent ghost callbacks
  if (state.ws) {
    if (
      state.ws.readyState === WebSocket.OPEN ||
      state.ws.readyState === WebSocket.CONNECTING
    )
      return;
    state.ws.onopen = null;
    state.ws.onclose = null;
    state.ws.onerror = null;
    state.ws.onmessage = null;
    state.ws = null;
  }

  try {
    state.ws = new WebSocket(buildWsUrl());
  } catch (e) {
    setWsUI(false);
    scheduleReconnect();
    return;
  }

  state.ws.onopen = function () {
    // Verify this is still the active WS
    if (this !== state.ws) return;
    setWsUI(true);
    state.reconnectAttempts = 0;
    console.log(
      "%c[WS] Connected",
      "background:#22c55e;color:white;padding:2px 6px;border-radius:3px;",
    );
  };

  state.ws.onclose = function () {
    if (this !== state.ws) return;
    setWsUI(false);
    scheduleReconnect();
  };

  state.ws.onerror = function () {
    if (this !== state.ws) return;
    setWsUI(false);
  };

  state.ws.onmessage = function (event) {
    if (this !== state.ws) return;
    var data;
    try {
      data = JSON.parse(event.data);
    } catch (_) {
      return;
    }
    handleServerEvent(data);
  };
}
```

---

## 누적 전체 버그 카운트

| 심각도    | 개수                                                                                   |
| --------- | -------------------------------------------------------------------------------------- |
| 🔴🔴 치명 | 1 (★ results.js 필드 불일치)                                                           |
| 🔴 중     | 9 (#1, #10, #14, #16, #17, #19, #25, #40, #51)                                         |
| 🟡 낮     | 18 (#2, #3, #4, #5, #7, #8, #9, #11, #13, #28, #30, #33, #35, #37, #41, #58, #59, #63) |
| ⚪ 미미   | 12 (#6, #12, #15, #18, #22, #24, #26, #27, #29, #38, #48, #61)                         |

**총 유효 버그: 40개.**

4차 검사에서 발견한 실질적 신규 버그는 **2개** (#51 UHF/UKS ndarray 처리, #63 WS ghost handler)였고, 나머지는 성능 이슈 또는 검증 후 버그 아님으로 확인되었습니다. 코드의 주요 경로는 이제 충분히 커버되었다고 판단합니다.
